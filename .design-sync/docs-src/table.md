A data table. `TableHeader` holds one `TableRow` of `TableHead` cells; `TableBody` holds the data rows.

Right-align numeric columns and add `tabular-nums` so digits line up. `TableCaption` describes what the rows are.
