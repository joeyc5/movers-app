An input with something attached: an icon, a unit, a button. `InputGroupAddon` takes `align` to sit at the start, end, or below the field.

Use `InputGroupInput` and `InputGroupTextarea` inside the group, not the plain `Input`.
