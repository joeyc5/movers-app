The form label. Point `htmlFor` at the control's id, or wrap the control so the click target extends across both.

Inside a form section, prefer `FieldLabel` so spacing matches the rest of the group.
