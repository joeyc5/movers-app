# Tracked on purpose. Both values are public: the URL is in every request
# the browser makes and the publishable key ships in the client bundle.
# Vercel builds read this file, so deploys need no dashboard env vars.
# Secrets and demo sign-ins stay in the gitignored .env.local, which
# overrides this file locally.
NEXT_PUBLIC_SUPABASE_URL=https://jannhzvqrsumtscidtkx.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_51FDbXTuZ5C8f7zTuajXQA_tlxe0dPl
